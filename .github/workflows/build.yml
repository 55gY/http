name: Build and Release

on:
  push:
    tags:
      - 'v*'  # 当推送以 'v' 开头的标签时触发，例如 v1.0.0
    branches:
      - main
      - master  # 支持main或master分支
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
    
    - name: 设置Go环境
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'
    
    - name: 下载依赖
      run: |
        go mod download
        go mod tidy
    
    - name: 构建Windows可执行文件
      run: |
        GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o http-windows-amd64.exe http.go
        GOOS=windows GOARCH=386 go build -ldflags="-s -w" -o http-windows-386.exe http.go
    
    - name: 获取版本号
      id: get_version
      run: |
        if [[ $GITHUB_REF == refs/tags/* ]]; then
          VERSION=${GITHUB_REF#refs/tags/}
        else
          VERSION="v$(date +%Y%m%d%H%M%S)"
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT
    
    - name: 创建Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.get_version.outputs.version }}
        name: Release ${{ steps.get_version.outputs.version }}
        draft: false
        prerelease: false
        body: |
          ## 📦 HTTP文件服务器 ${{ steps.get_version.outputs.version }}
          
          ### 下载说明
          - **Windows用户**: 下载 `http-windows-amd64.exe` (64位) 或 `http-windows-386.exe` (32位)
          
          ### 使用方法
          ```bash
          # Windows
          http-windows-amd64.exe -port 127.0.0.1:8080
          ```
          
          ### 新功能和改进
          - 快速启动本地HTTP文件服务器
          - 支持自定义监听地址和端口
          - 自动记录访问日志
        files: |
          http-windows-amd64.exe
          http-windows-386.exe